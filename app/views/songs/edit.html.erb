<h1>Edit <%= @song.title %> </h1>
<p>Add song for <%= @artist.name %> </p>
<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <h2><%= pluralize(@song.errors.count, "error") %> prohibited this record from being saved:</h2>
    <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.submit %>
<% end %>
